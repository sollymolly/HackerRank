N = int(input())
arr = input().split()
for i in range(len(arr)):
    arr[i] = int(arr[i])
arr = sorted(arr)
maxi = arr[N-1]
while True:
    if arr[len(arr)-1] != maxi:
        break
    arr.pop()
print(arr[len(arr)-1])
    
